{
  "name": "@app-test-ctrl/code-analyzer-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for static project analysis — discover docs, detect platform, extract pages/handlers/apis/routes from source. Used by smart-qa skill to infer business flows.",
  "bin": {
    "code-analyzer-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
